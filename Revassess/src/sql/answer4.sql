SELECT * FROM STUDY_SET CROSS JOIN (SELECT * FROM FLASHCARD WHERE flashcard_id IN (SELECT * FROM STUDY_SET_CARD WHERE STUDY_SET_ID.study_set_id = STUDY_SET_CARD.study_set_id)) where APP_USER.user_id = 4;